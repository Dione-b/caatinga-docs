---
interface Props {
  size?: "sm" | "md" | "lg" | "xl" | "full";
  class?: string;
}

const { size = "lg", class: className } = Astro.props as Props;

const widths: Record<NonNullable<Props["size"]>, string> = {
  sm: "max-w-3xl",
  md: "max-w-5xl",
  lg: "max-w-7xl",
  xl: "max-w-[105rem]",
  full: "max-w-none",
};
---

<div class:list={["mx-auto w-full px-4 sm:px-6 md:px-8", widths[size], className]}>
  <slot />
</div>
