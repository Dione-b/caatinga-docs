---
import Alert from "@components/Alert.astro";

interface Props {
  title?: string;
}

const { title = "Tip" } = Astro.props as Props;
---

<Alert variant="success" title={title} icon="lucide:lightbulb">
  <slot />
</Alert>
