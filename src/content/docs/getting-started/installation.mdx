---
title: Installation
description: Requirements and installation instructions for the Caatinga CLI.
sidebar:
  order: 2
---

<Note title="Work in progress">
  This page is a stub from Sprint 3 — the full Getting Started section lands in Sprint 9.
</Note>

Install the Caatinga CLI globally, or run it directly with your package manager's dlx command.

## Requirements

- Node.js 22 or later
- Stellar CLI installed and on your `PATH`

## Install the CLI

<CodeTabs
  npm="npm install -g @caatinga/cli"
  pnpm="pnpm add -g @caatinga/cli"
  yarn="yarn global add @caatinga/cli"
/>

### Without a global install

Both the `caatinga` and `ctg` aliases also work through your package manager's runner, e.g. `npx caatinga doctor`.
